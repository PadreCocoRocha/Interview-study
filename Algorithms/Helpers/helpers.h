#ifndef __HELPERS_H
#define __HELPERS_H
/*
	prints an array of integers with the format [i1,i2,i3,...,in]
*/
void printRangeInt(int* arr, int start, int stop);

/*
	prints an array of chars with the format [i1,i2,i3,...,in]
*/
void printRangeChar(char* arr, int start, int stop);

#endif /* __HELPERS_H */